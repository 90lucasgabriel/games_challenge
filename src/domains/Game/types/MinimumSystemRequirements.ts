type MinimumSystemRequirements = {
  os: string;
  processor: string;
  memory: string;
  graphics: string;
  storage: string;
};

type MinimumSystemRequirementsRaw = {
  os: string;
  processor: string;
  memory: string;
  graphics: string;
  storage: string;
};

export type { MinimumSystemRequirements, MinimumSystemRequirementsRaw };
